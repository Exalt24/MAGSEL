# MongoDB Atlas credentials for the logincluster / Metaverse database.
# Real values live in your MongoDB Atlas console (Database Access) — rotate/read there.
DBUSER =
DBPASS =

# Optional: defaults to 3000 if unset (see index.js)
PORT =
